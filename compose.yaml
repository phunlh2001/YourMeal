version: '3.9'

services:
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - data:/data/
      - ./database/initdb.js:/docker-entrypoint-initdb.d/init-db.js:ro
    environment:
      MONGO_INITDB_DATABASE: yourMeal

volumes:
  data: {}
